spring:
  application:
    name: security
  config:
    import: "configserver:"
  cloud:
    bus:
      enabled: true
    config:
      fail-fast: true
      name: security, rabbitmq-common, eureka-common, ribbon-common, resilience4j-common
      discovery:
        enabled: true  # 启用服务发现来找到 Config Server
        service-id: config